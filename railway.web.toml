# Serviço Web — Config File Path: /railway.web.toml
# Também defina a variável: RAILWAY_DOCKERFILE_PATH=Dockerfile.web
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
startCommand = "npm run start:web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
