# Serviço Web — Config File Path: /railway.web.toml
# Variável extra (recomendado): RAILWAY_DOCKERFILE_PATH=Dockerfile
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm run start:web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
