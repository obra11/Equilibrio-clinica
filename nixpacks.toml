# Usado pelo serviço Web (Nixpacks). A API usa Dockerfile via railway.toml.
[phases.setup]
nixPkgs = ["openssl"]

[variables]
NPM_CONFIG_PRODUCTION = "false"
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci"]
